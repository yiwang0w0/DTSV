# Readme_GPT
## 2026-03-21 / 清理旧实现与不可达代码
- 状态：已从 `src/lib/server/gameActions.js` 删除确认不再使用的旧实现与不可达分支。
- 本轮清理：
  - 删除旧的 `searchAreaImpl / attackNpcImpl / fleeNpcImpl / attackPlayerImpl / useItemImpl`
  - 删除五个动作 wrapper 里 `return` 之后的整段不可达旧逻辑
  - 连带删除只为旧逻辑服务的 `applyTurnEffects()` 与 `ensureCorpsesForNewDeaths()`
- 结果：
  - `gameActions.js` 现在只保留当前事件结算器所需的主链路实现
  - 本轮共净删约 700+ 行旧代码
  - 服务端动作入口更清晰，后续继续按模块拆分会更容易
- 验证：
  - `node --check src/lib/server/gameActions.js` 通过
  - `npm run smoke` 通过

## 2026-03-21 / 全局 debug 巡检
- 范围：对当前工作区做一次整体 debug，重点检查 `src/lib`、`src/app/api`、smoke、以及构建依赖环境。
- 已修复：
  - `src/lib/server/gameActions.js` 补回 `applyTurnEffects()` 与 `ensureCorpsesForNewDeaths()` 兼容 helper。
  - 目的不是回退事件结算器，而是给仍留在文件里的旧实现残段补护栏，避免这些旧路径在被误调用时直接因为 helper 缺失而报错。
- 当前代码检查结果：
  - `node --check` 已通过 `src/lib/**/*.js`、`src/app/api/**/*.js`、`scripts/*.mjs`
  - `npm run smoke` 通过
  - 现阶段没有发现新的语法级错误或 smoke 回归
- 当前环境问题：
  - `npm run build` 失败，原因是本地没有可执行的 `next`
  - `npm run lint` 失败，原因同上
  - `npm ls next` 与 `npm ls playwright` 都是空，说明当前工作区未安装对应依赖
- 结论：
  - 现在主风险不在代码语法，而在本地依赖环境缺失
  - `gameActions.js` 依旧偏大，后续仍建议继续删除旧残段并按模块拆分

## 2026-03-20 / 事件结算器与地图配置整理
- 状态：已新增 `src/lib/eventResolver.js`，作为当前服务端动作层的轻量事件结算器。
- 已接入动作：
  - `search`
  - `attackNpc`
  - `flee`
  - `attackPlayer`
  - `useItem`
- 统一结算内容：
  - 回合开始 Buff 结算
  - 新死亡玩家的尸体生成
  - 玩家状态写回
  - 共享日志累积
- 地图配置：
  - `src/app/admin/_tabs/MapsTab.jsx` 已移除 `danger_level` 编辑项。
  - 当前 `src/` 内已无 `danger_level` 引用，可视为废弃冗余配置。
  - 假设本轮先做代码层移除，数据库里的 `map_config.danger_level` 列暂未执行删库。
- 验证：
  - `node --check src/lib/server/gameActions.js` 通过
  - `node --check src/lib/eventResolver.js` 通过
  - `npm run smoke` 通过
  - `npm ls playwright` 显示本地未安装
- 待继续：
  - `gameActions.js` 里仍保留旧实现残段，后续可继续按动作模块拆分
  - 尸体/事件结算还缺专项 smoke
  - 当前环境仍未完成 `build` / `lint` / Playwright 浏览器回归

## 2026-03-20 / 尸体战利品系统更新

- 状态：已实现第一版尸体搜刮链路，方向改为“尸体保留剩余物资”，不做随机装备掉落。
- 服务端：
  - `src/lib/server/gameActions.js` 新增尸体发现、即时战利品 prompt、`lootCorpse`、`dismissLootPrompt`。
  - `search / attackNpc / flee / attackPlayer / useItem` 现在都会在死亡时同步尸体状态。
  - 玩家尸体继续以 `equipment_instances` 为装备事实源；拾取装备时会做 owner 转移或实例生成，并在 `persistRoom()` 失败时回滚。
- 前端：
  - 新增 `src/app/game/[id]/LootModal.jsx`。
  - `src/app/game/[id]/GameClientPage.jsx` 已接通拾取弹窗、关闭动作和当前地图尸体提示。
- 规则：
  - NPC 或玩家死亡后，击杀者可立即从尸体中带走 1 件装备或道具。
  - 尸体剩余内容会继续留在地图上，后续玩家通过搜索还能继续拿。
- 验证：
  - `npm run smoke`：通过。
  - `node --check src/lib/server/gameActions.js`：通过。
  - `node --check src/lib/roomState.js`：通过。
  - `npm run lint`：当前环境失败，因为没有可执行 `next`。
- 待继续：
  - `scripts/smoke-check.mjs` 还没补到尸体搜刮专项断言。
  - 本地仍未完成 `next lint` / `next build` / 浏览器联机回归。


更新时间：2026-03-19（Asia/Tokyo）
整理方式：基于当前仓库源码与本轮并发修补结果整理；已完成本地 smoke 与语法级校验，未完成完整构建与浏览器联机回归。

## 当前状态

项目现在已经是“主链路基本闭环的 v1 雏形 + rooms 乐观锁一期收尾版”。

相比 2026-03-18 的版本，本轮重点补的是并发写安全：
- `rooms` 表已经要求存在 `version integer not null default 0`
- 游戏动作与装备动作的 `rooms` 更新已基于 `id + version` 做乐观锁更新
- 服务端版本冲突会返回 `409`
- 客户端遇到 `409` 会自动短重试

## 本轮新增 / 修正

- 并发安全收尾：
  - `src/lib/server/gameActions.js`
    - `persistRoom()` 使用 `eq('version', currentVersion)` + `version + 1`
    - 保留 `VersionConflictError` 和 `withRetry()`
    - NPC/PvP 战斗的耐久扣减已移动到 `persistRoom()` 成功之后执行，避免版本冲突时重复扣耐久
  - `src/lib/equipmentEngine.js`
    - `executeCraft()` 现在会记录合成副作用 journal
    - 如果 `rooms` 持久化失败或发生版本冲突，会回滚：
      - 新插入的装备实例
      - 被消耗的前置装备实例
      - 失败降阶时被扣掉的前置耐久
    - 新增 `rollbackCraftSideEffects()`
- 管理后台不再绕过乐观锁：
  - `src/app/admin/_tabs/RoomsTab.jsx` 已从前端直连 Supabase 改为调用 `/api/admin/rooms`
  - `src/app/api/admin/rooms/route.js` 新增 `PATCH`：
    - 管理员可强制结束房间
    - 强制结束会写共享日志
    - 强制结束也会带 `version` 检查
- 协作记录：
  - 新增 `progress.md`，记录本轮问题、修补点和验证结果

## 已完成

- 大厅创建房间、加入房间、进入游戏、共享日志、基础战斗、PvP、道具使用、装备合成、穿戴/卸下、管理员后台等主流程继续可用。
- 关键写操作仍统一走 Next API：
  - `POST /api/game/rooms`
  - `POST /api/game/actions`
  - `POST /api/game/equipment`
- `rooms.gamevars.log` 仍是共享日志单一来源。
- `validnum / alivenum / deathnum / winner / gamestate` 仍由服务端统一维护。
- 根目录旧版重复文件仍已删除，现用代码以 `src/` 为准。

## 仍需优化

- 当前乐观锁只保护 `rooms` 这一层，尚未上 Supabase RPC / Postgres 事务级原子方案。
- `gameActions.js` 仍然偏大，后续可以继续按房间、战斗、道具、装备拆分。
- 装备穿戴/卸下目前虽然已不再绕过房间版本写入，但 `equipment_instances` 自身仍不是事务式更新。
- 还没有真正的端到端浏览器回归；本地当前缺少 Playwright 包。
- 仍需要一次完整的 `npm run build` / 联机实测来确认本轮没有隐藏回归。

## 未实现

- 随机装备掉落
- 排行榜 / 战绩沉淀 / 局后奖励
- 深度移动端适配
- 基于 RPC/事务的彻底并发保护
- 断线重连机制
- 完整端到端自动化测试

## 验证结果

- `npm run smoke`：通过
- `node --check src/lib/server/gameActions.js`：通过
- `node --check src/lib/equipmentEngine.js`：通过
- `node --check src/app/api/admin/rooms/route.js`：通过
- Playwright：本地未安装，无法执行浏览器自动回归

## 当前关键入口

- `src/lib/server/gameActions.js`：游戏动作、版本冲突、房间持久化
- `src/lib/equipmentEngine.js`：合成执行、合成回滚、耐久逻辑
- `src/app/api/game/actions/route.js`：动作 API
- `src/app/api/game/equipment/route.js`：装备 API
- `src/app/api/admin/rooms/route.js`：管理员删除 / 强制结束房间
- `src/app/admin/_tabs/RoomsTab.jsx`：后台房间管理 UI
