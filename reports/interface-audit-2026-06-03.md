# 全界面走查审计 — 2026-06-03

> 触发：用户「对所有界面走查+测试，主要是名词防割裂、按钮响应速度、功能可用、有无报错」。
> 方法：① 浏览器运行时走查（Chrome MCP · dtsv.vercel.app · 登录态 kanata）② 全界面只读代码审计（workflow，5 分组 + 汇总）。
> 红线：所有修复**仅前台文案/标签**；残片可发现性、Ω/污染服务端机制、深层 codex 内容不动（守 narrative-vision §6.1 Additive Evergreen）。

## 一、运行时走查（浏览器）— 健康

- 走查 首页 / BR 对局(/game/22) / 大厅(/rooms) / 档案库(/archive)：**全部正常加载**，无白屏/崩溃。
- 网络**全 200**（含 nav 对 stash/codex/contracts/profile/admin 的 _rsc 预取），**无 4xx/5xx**。
- **无 console 报错**。
- 响应速度：页面加载 ~3-4s（免费服务器冷启动）；对局动作之前实测 **6.9s**（gamevars 膨胀，瘦身 `d296d4e` 已推送，待 Vercel 部署复测）。

## 二、名词割裂（核心问题）

**割裂几乎全部集中在 `/game/[id]`（GameClientPage.jsx）**——它是 BR 实际运行的对局界面，但仍把旧 chamber 时代的**远星表层符号**与新 BR **中性术语**混在同一屏。次级割裂在两个前台漏斗 `/`(首页) 与 `/rooms`(大厅)。
**正面（无需改）**：`/codex`(纪元档案) 与 `/archive`(档案库) 是正确的远星深层落点（=该保留的 20%）；独立 `/br` 页、`gameUi.js` BR 组件、`br_rooms` 种子、`src/lib/server/br/*` 已是干净中性术语，可作前台对齐范本。

### 统一术语表（glossary）

| 概念 | 当前混用 | 推荐统一（前台） |
|---|---|---|
| 战场/对局容器 | 远星函馆·17号异常段 / 现役异常段 / 虚拟空间(已对) | **虚拟空间实例 #N**（远星函馆退为站点 masthead 品牌；17号异常段退深层） |
| 房间/格子 | 扇区(对) / chamber / 当前地图 | **扇区**（区段=分区；停用「chamber/地图」前台文案） |
| 缩圈机制 | 时空收缩(发明词) / 收缩 / 缩圈 / 禁区 | **收缩 / 收缩边界 / 禁区**（删「时空收缩」，面板标题改「收缩边界·扇区图」） |
| 时间层/深度 | 深度/有效阶段/真实阶段/时序跃迁(已自洽) | 锚定保留：**深度 / 有效阶段 / 真实阶段 / 跃迁 / 时序跃迁器** |
| 撤离 | 撤离(对) / 结构退避 / 缝隙维护轨道 / 外环维护廊 | **撤离 / 紧急撤离**（停用结构退避/缝隙维护轨道/外环维护廊前台文案） |
| 时间压力 | 大时钟(对) / Ω倒计时(已 dormant) / 污染·张力警报 | **大时钟/收缩阶段/末路阶段**（污染见 §四决策） |
| 异步遗留物 | 异步探针·PI引导者·残片碎片 | **跃迁者残影**（书写者/跳跃者框架；PI引导者退深层） |
| 可拾取物资 | 物资档 T1-T5(对) / 结构碎片·环段部件·Ω物质 | **物资档 T1-T5**（远星品类名降为 item flavor，不作前台骨架；残片 UI 收进深层） |
| 玩家身份/单位 | 引导者 / 玩家(对) | **玩家**（引导者/PI引导者退深层） |
| 一局/计量 | 周目(单人 roguelike) / 对局(BR, 对) | **对局**（周目限定跨局元进度语境） |

### 割裂清单（按严重度）

- **[严重] 核心割裂**（用户点名）：`/game` 头部硬编码「远星函馆·17号异常段」(L1077) + 右栏「🕒 时空收缩·扇区图」(L1794) 同屏——深层远星战场名 + 发明词「时空收缩」+ 中性「扇区图」三种语域叠在一块屏。
- **[严重] 双时间压力并存**：BR 大时钟 + 污染「张力警报」红条(L994) + 90% 强制撤离模态「泡泡壳裂解临界…缝隙维护轨道」(L1043-1066)——两套互相打架的时间压力 + 深层 canon 撞中性 BR。
- **[严重] 异步探针卡**(L1540-1561) 前台直挂「另一位 PI 引导者留下·携 N 残片·残片碎片」，与 BR「扇区/跃迁/深度」新框架脱节（§5 早已规划重构为「跳跃者残影」，UI 未跟进）。
- **[中] 紧急撤离链路全远星**：按钮「⚠ 缝隙维护轨道」/「🚪 结构退避」/ toast「已传送至外环维护廊」——高频功能按钮需一眼可懂。
- **[中] 左右栏分裂**：左栏区域评估/PvP「当前地图/同地图」vs 右栏「扇区」指同一位置。
- **[中] 首页/大厅整屏远星**：首页「第六纪元·深界时代·PI引导者协议·泡层文明残响」；大厅「周目记录·17号异常段·净化失败·引导者 N」——都直接路由进已 BR 化的 /game（违反 §6.1 新手不前置六纪元谜题）。
- **[轻] 物资双轨**（网格 T3 vs 背包 Ω物质）/ 残片 UI 仍在前台 / 跃迁网格「阶段 N 禁区图」未标注"我读的层 vs 世界真实层"。

## 三、功能 / 报错

- **报错**：未发现运行期报错/崩溃（运行时网络全 200、无 console 错；代码 BR 派生计算均有防御兜底）。
- **功能**：① **双时间压力重叠**需产品决策（污染在 BR 保留为风味 or 移除前台横幅）；② 左栏区域评估 BR 下应「当前扇区」非「当前地图」；③ 跃迁网格标题缺"我读的层"标注（可用性，非 bug）。

## 四、优先级修复清单

> 全部前台文案/标签级，低风险；按 glossary 执行。

- **P0**（核心割裂·用户点名）：`/game` 头部 → 「虚拟空间实例 #N」；右栏面板标题删「时空收缩」→「收缩边界·扇区图」。
- **P1**（明显不一致）：异步探针卡 → 跃迁者残影框架；紧急撤离/撤离按钮 → 中性「撤离/紧急撤离」；左栏「当前地图」→「当前扇区」；首页/大厅前台 → 虚拟空间化（远星退深层）；引导者→玩家、周目→对局。
- **P2**（润色）：物资双轨统一 T 档为前台骨架；残片前台钩子收轻；跃迁网格阶段标注"我读的层"。

**待产品决策（§四功能①）**：BR 下污染怎么处理——保留为去远星化的「个人态压力」风味 / 移除前台横幅（大时钟为唯一压力）/ 保留原样。
