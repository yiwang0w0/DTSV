# KALEIDO · 05 渐进披露(UI 即进度)+ 世界线定向(2026-07-07 · Kanata 拍板)

> 来源:Kanata 于 📖 剧情会话直接定向(拍板级),经 🧭 固化。本文件 = 设计捕获 + 给 📖 N3 的接口形状;完整拆解与派单在各轨恢复后由 🧭 追加。
> **正史源变更**:剧情正本 = `D:\Fragments\Farstar_Hakodate_作品_20260509`(唯一正史,仓库外);仓库 `docs/narrative-vision.md` 分期与作品不符,已授权 📖 修订(宪法条款全部保留)。

## 0. Kanata 四条定向(原样固化)

1. **开局无 UI,只有一个搜索按钮;UI 随游戏进行渐进丰富**(A Dark Room 式「UI 即进度」)。
2. **世界线锚定第三纪元(失衡时代)**:玩家探索函馆内部构造 + **π-段**(与小说 Ω-段正史隔离)。
3. **玩家身份 = 结构工程体**(维护单位);KALEIDO 玩家**不再称「引导者/PI」**(时序勘正:PI 岗位拓荒时代才启用)。现有 kaleido UI 文案中的相关字样待 📖 N3 给替换文案后清理。
4. **写作铁律:少专有名词,别让玩家看不懂** —— 全部玩家可见文本适用(细则 = 📖 N1 §3 六条)。

## 1. ui_unlocks 机制(设计草案 · 🔧/🎨 恢复后细化实现)

### 1.1 解锁条目形状(📖 N3 按此写文案)

```json
{
  "ui_key": "hp_bar",
  "trigger": { "verb": "fight_start", "condition": null, "timing": "before" },
  "nar_line": "……(📖 N3 供稿:该 UI 浮现时日志里出现的那一行)",
  "precedes": ["首次可受伤"],
  "note": "时序法则锚点"
}
```

- `trigger.verb` 复用传感层动词(search/fight_start/attack/level_clear/item_use/craft_attempt/death…),`timing: before|after` 表示在该事件**结算前/后**浮现(时序法则用 before)。
- `nar_line` 走数据不硬编码进客户端组件(📖 供稿)。**存储裁决(2026-07-07 🧭,采 🔧 契约方案 A)**:P1 = 引擎侧 data-only 注册表(uiUnlocks.js,📖 供稿逐字进入,改文案=改数据文件不动逻辑,客户端只消费信封下发的服务端权威 nar_line);P2 迁 content_pool(admin 可改)。文案变更流:📖 改 docs 供稿 → 🧭 → 🔧 同步注册表。
- **持久化 = 账号级跨会话**(Kanata 明确):首个 run 体验完整渐进;后续 run 继承已解锁集(收敛/permadeath 不回收 UI —— UI 是元进度,不是 run 内容,兼容 R8/R9)。落库形状(🔧 定稿):倾向 profiles 加 jsonb 列或轻表 player_ui_state。

### 1.2 硬时序法则(不可违反)

**任何能伤害玩家的机制,其读数 UI 必须先于首次伤害解锁**;首次战斗必须安全上演(hp_bar 先于第一次可能掉血出现;R6 规则卡先于对应规则首次生效——与规格 R6「生效前展示」同源)。

### 1.3 ui_key 候选清单(🧭 草拟 · 📖 按此写 nar_line,🎨/🔧 恢复后校订)

| ui_key | 解锁时机(草案) | 备注 |
|---|---|---|
| search_btn | **初始唯一 UI** | — |
| log_panel | 首次 search 后 | NAR 通道随第一次搜索醒来 |
| inventory | 首次获得道具 | |
| hp_bar | 首次遭遇建立**前**(timing=before) | 时序法则锚点 |
| combat_panel | 首次遭遇(安全上演) | |
| move_btn | 首次 level_clear | 「可以往前走了」 |
| level_header | 首次 move 后 | 第 N/5 关概念此刻才浮现 |
| turn_counter | 与 level_header 同批或首个 survive_turns 关 | |
| rules_card(R6) | 首个带规则覆盖/战斗模板的关**入关前** | 时序法则 |
| stance_ui | 首个 stance_duel 精英关 | |
| craft_btn | 首次拾得配方材料 | |
| convergence | 版本终止(通关/死亡)常驻 | 不属解锁物 |

## 2. 对现有 KP1 派单的冲击评估(恢复令时重排)

| 轨 | 冲击 |
|---|---|
| 🔧 引擎 | 新增:ui_unlocks 触发判定(路由边界/传感层复用)+ 账号级持久化 + 解锁事件下发;E2E 增解锁序断言。原 KP1-E(LW-3/D3/D5)不变但排序或后移 |
| 🎨 前端 | **结构级**:kaleido 全部 UI 件包条件渲染(关卡头/R6 卡/背包/战斗面板全变成解锁物);初始态 = 一个搜索按钮 + 渐次浮现动效;1b 三态 UI 变为 stance_ui 解锁物 |
| ⚙️ 内容数值 | 解锁节奏与内容投放耦合:D6 种子关需保证解锁触发链在 seq1-2 自然发生(首道具/首遭遇/首配方材料的投放下限) |
| 📖 剧情 | N3 = 挂载点清单 + **每个 ui_key 的 nar_line**(按 §1.1 形状);「引导者/PI」字样替换文案 |
| 闸门 | **P1 闸门增补第 6 条:「到第二个 UI 元素的时间」指标**(首个 run 实测);连同第 5 条「不哑」构成叙事/体验双指标 |

## 3. 世界观定向的落地指针

- 锚点:第三纪元(失衡时代)·函馆内部构造·**π-段**(游戏自有异常段编号,与小说 Ω-段隔离,避免正史冲突)。
- 双层制(驻点持久层 + 远征生成层)已入 📖 N1;玩法侧结构意见由 ⚙️/🔧 恢复后与 📖 对齐(经 🧭)。
- 现有种子内容(npc/item/chamber 文案)在 N1 定位下的复用度以 📖 N1 评估为准。
