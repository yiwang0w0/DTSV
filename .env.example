# ── Supabase ──
# 公开（内嵌客户端包，浏览器可见）：URL + anon key
NEXT_PUBLIC_SUPABASE_URL=https://thlapfhxysfmjpjjpmmh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
# 机密（仅服务端·绕过 RLS·禁止 NEXT_PUBLIC_ 前缀·禁止进任何客户端组件）
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ── Anthropic（KALEIDO P2 内容生成用·目前未启用，先立规矩）──
# 机密密钥。约定（🔒 KP0-X #4）：
#   1. 仅服务端使用 —— 只在 src/lib/server/generation/ 内读取，禁止 NEXT_PUBLIC_ 前缀。
#   2. 禁止进任何客户端组件 / 'use client' 文件 / API 响应体 / 日志。
#   3. 生产环境经 Vercel 环境变量注入，绝不提交真实值（本文件仅占位）。
ANTHROPIC_API_KEY=your_anthropic_api_key_here
