# 🧭 停工交接 · 2026-07-23

> **本文件 = 恢复时的第一个入口。** 每次停工由 🧭 覆写。读完这一份 + `Readme_Claude` 就能接着走。
> main 停在 **`c131469f`**(以 `git log origin/main` 为准 —— 停工收口期各轨仍在推,本文件被多轨订正过)。工作区唯一 untracked = `scripts/phase-25q-nemesis-pvp-death.sql`(**故意保持**,勿提交)。

---

## 0. 一句话

**KALEIDO(远星)主线在「step0/step1 阶段制 + Diegetic UI 教义」这套新结构上重建;设计层已定案并落档,实现层刚补完地基的一半。**
本轮把「触发到底产出什么」这个病根解了(**要约/发现** —— 05 的触发从来不是「授予 UI」),把三条持久线分清了(UI 解锁随存档点回滚 / 场景状态跨单位不回滚 / step 标记永不回滚),补齐了引擎侧的负伤流血与周期保底,修了四个静默失效的 bug,建了门(`npm run gate`)。

⚠ **但 step1 现在还跑不起来**:⚙️ 停工时自查发现**整条药品经济在线上是断的**(13 件 kaleido 道具一件都搜不到)⟹ `kaleido_bleed_per_action` 在链修好前**不能打开**。详见 §3-1 那两条 🔴。
⚠ **玩家可见行为几乎没变** —— 新机制全部默认关。这一轮是**设计定案 + 地基**,不是功能上线。

---

## 1. 各轨状态

| 轨 | session | 停工时手上 | 恢复第一步 |
|---|---|---|---|
| 🔧 引擎 | `local_4c83114c-4531-47a5-b4a2-3260d384c2ef` | ⚠**本行 2026-07-23 由 🔧 订正**(停工令与我的交付消息交错，原写「`d` 未开工」不实)。已交:周期保底 / 道具链三处 / P0 gate 9 步 / H3 / 教义不变式门 / 条件 A·B·A3 / doc 13 勘察 / doc 14 字段落点草案 / doc 15 H1 通道设计 / **`d` 负伤流血(`0534d9f3`·E2E 91/91×3)** / `kaleido_scene_state` DDL(已写·**未跑**)。**真正未开工**:P1 投影重构、E2E 碰 profiles | ⚠ **等 🧭 批 doc 14 的四件** → 开 **P1**。⚠ **`d` 已完成，恢复时不要重做**(⚙️ UPSERT 两行 `game_rules` 即开) |
| ⚙️ 内容数值 | `local_02f00369-c121-4e5e-bb48-ad34dae8bb5a` | 曲线**已重推完**(非半截·`ec817a17`)+ 动作构成套表 + statδ 补丁出稿 + **两条 🔴 自查发现**(§3-1)。顺带修了 harness 自身一个 bug:保底曾被写成概率 `Math.random()<1/G` 而非确定性计数器 —— **那恰好摧毁了保底存在的唯一理由(最坏情况保护)**,现有数是修后的 | ⚠ **恢复第一步已变**:不是「用药策略敏感度」,是 **解物品可达性**(§3-1 那条 🔴)。修法动**多人共享表** ⟹ **卡 🧭/Kanata 拍**,⚙️ 不自己动。另等 Kanata 拍死亡率取舍(§2) |
| 📖 剧情 | `local_6f8937f6-e881-4f0e-9a6d-66815241ac60` | N1-N9 全交付,四项裁决 + 同率全收口。**停在 step1 全量开工前一刻,零半截活** | **写 step1 全量文案** —— 只需读 **N9 §3(四拍骨架)+ §6(交接清单)**,不必回溯讨论 |
| 🎨 前端 | `local_1d2b3cbf-ee47-4d66-841e-f0c74e92e5a3` | BUG-A/B 已推 + `smoke-immersive-route.mjs`(3 格负对照)+ **SSR 顶栏闪判定不治并记档翻案条件** + 六条上下文判断已落盘 | 等 P1-c(**必须与 🔧 同 PR**);其前置两件(移除/被夺呈现语言草案、`statusActionReady` 逐行门解耦范围)恢复后立刻做 |
| 🔒 安全 | `local_35e56c6e-13e0-493c-85da-d843d671eaba` | **无在飞任务**。ui_unlocks 列级守卫已上线且形状无关 | 无。新 DDL 出现时再拉 |

### 🧭 恢复后的第一件事(在派任何单之前)

**读 `docs/plan/kaleido/14-field-layout-draft.md` 并一次性批四件**:①存档点提交要不要单独 retry ②`hide`/`restore` 跨轨同批窗口(**方向已给:同批约束成立,不得先行下发**)
   > **精确理由(🎨 指出,比 §5「互相打脸」准)**:`hide`/`restore` 带顶层 `ui_key`,而今天的客户端消费端是**无条件 `keys.add`** ⟹ **「失去一件 UI」会被渲染成「解锁一件 UI」,方向反了。** 这也正是「渲染判据要可收缩」这条的落点。③`profiles.kaleido_step` DDL ④evaluator 共用程度。
🔧 **批复前不动 P1 代码**,所以这是全链的第一个闸。

⚠ `send_message` **必须传完整 UUID**,短前缀报 `not found`。

---

## 2. 挂在 Kanata 身上的(唯一阻塞源)

**线上验收四条** —— 各轨本地占位 env 验不了真实账号态:

1. 转场只播一次(不再反复播图1画面)
2. 对局页无顶栏 —— ⚠ **重点看 `/login` 那一格**:🎨 判断 Kanata 看到的主要是登录卡片页上的顶栏,它在时间轴上比首页更早
3. 第二次进入不重播觉醒行 + 状况面板不再飞一遍
4. **新号首搜后 `log_panel` / `hp_bar` 是否照常浮现** —— 🎨 关停 stub-derive 后,服务端是否真在发这些键,只有线上能验。**不浮现 = 服务端没发,要 🔧 补**
5. **admin「道具池」tab 扫一眼**(🔧 自报未验:登录管理员账号需输密码,**不在任何轨的可执行范围**)。预期:加力件显示 `ATK +2` 走新列、旧列灰显「ATK(旧) 2」带 tooltip

### 🔴 新增:两个设计取舍(⚙️ `ec817a17` 提,停工时未拍)

**① 死亡率取舍 —— 「又薄又零死亡」在新掉血口径下不再同时可得**

我批「每个消耗性动作都在流血」之后,⚙️ 按新动作面重跑,**原定标全部失效**:动作数从 30(纯搜)涨到 ~41,总耗血 **+37%**,同参数死亡率从 **0% → 30%**。

| 配置 | 档1 | 档2 | 档3 | 死亡率 |
|---|---|---|---|---|
| d=4 G=12 p=.08 | 91% | 56% | 15% | **6%**(张力回到原形状) |
| **d=4 G=10 p=.08** | 87% | 48% | 11% | **3%**(⚙️ 推荐) |
| d=3 G=18 p=.05 | 74% | 37% | 5% | **0%**(零死亡但张力明显变薄) |

⚙️ 的话:「**这是口径变更的真实代价,不是调参没调好**」;推荐 3%,理由是「让『不打算让玩家死』保持为**倾向**而非**保证**」。
⚠ **但 Kanata 的原话是「说实话 step0 到 1 的过程中我没打算让玩家死亡」** —— 3% 与之有张力,**必须他拍**。
🧭 未表态。若他坚持零死亡,代价是档2 从 ~50% 掉到 37%(「过半的局真紧张过」这个形状没了)。

**② 战斗仍是严格劣势 —— 我原来担心的问题没被真正解决**

⚙️ 的连带发现:新口径堵上了 `releaseEncounter` 的免费漏洞,**但躲战仍便宜 4 倍**(放过 = 1 动作 4HP;打一场 ≈ 4 动作 16HP),而 step1 的药靠**搜**不靠**打** ⟹ **理性玩家仍然永不战斗,只是从「免费不打」变成「花小钱不打」**。
⚙️ 建议:**击杀掉落药/材料**(~50% 掉一瓶 heal30 ⟹ 期望 +15 vs −16 ≈ 打平)⟹ fight-vs-flee 才成为真决策。叙事上也顺(从放倒的那东西身上捡到能用的)。**会牵动 📖 的战斗叙事与掉落文案。**

**除以上两条,设计层没有卡在 Kanata 身上的东西。**

---

## 3. 已定但未落地(实现队列 · 按优先级)

### 1. ✅ `d` 掉血机制 —— **已落地**(`0534d9f3` · E2E 91/91×3 · 停工令到达前完成)
- ⚠ **本节 2026-07-23 由 🔧 订正**:原写「唯一缺的地基/引擎侧零实现」已不成立。**机制已就位,恢复后不要重做。**
- **⚙️ 恢复后开启方式**:`game_rules` UPSERT 两行 `kaleido_bleed_per_action` / `kaleido_bleed_jitter`(默认关 ⇒ 现状零行为变化),**不需要 🔧 发版**。
- **实现符合下列全部口径**(逐条自查):每消耗性动作都流 ✓ / `releaseEncounter` 纳入且 kaleido-scoped ✓ / **全程同率(单值 + 方差,非分段)** ✓ / `game_rules` 播种进 `gamevars.kaleido.bleed` ✓。
- **🔧 自行判定的一处,待 🧭 追认**:`releaseEncounter` **流血但不计回合**(计回合会改 `survive_turns` 清关速度 = ⚙️ 没要的平衡变更)。要改成也计回合是一行,但请先让 ⚙️ 认平衡影响。
- **一个结构性风险已处理**:流血致死必须**当拍收敛 run**,否则玩家死了但 run 仍 `active`,而各 handler 对阵亡玩家首行 `throw` ⇒ 他再进不来 ⇒ **run 永久悬空**。已提 `convergeDead` 局部函数共用。
- **⚠ 口径已改**:不是「每次搜索扣 HP」,是「**负伤在持续流血,每个消耗性动作都在流**」(`TURN_ACTIONS` 那一族)。
- **连带**:`releaseEncounter` 必须纳入 kaleido 的消耗性动作(**kaleido-scoped**,多人守 Phase 37)—— 它现在是零回合成本,等于在计时器上开洞,理性玩家永不战斗。
- **费率全程同率**(`12 §1.3`):N7 的余量锚点公式假设单一费率,分段会让阈值在段边界错档。
- 数值从 `game_rules` 播种进 `gamevars.kaleido`,**与周期保底同范式**(路已铺好)。
- 分工:机制=🔧 / 数值=⚙️(重推中)/ 叙事框架=📖(⚙️ 去会签,已批)。

#### 🔴🔴🔴 但**现在还不能打开** —— ⚙️ 停工后自查(`87519616`)

**整条曲线校准在一个线上不存在的药品经济上。** 五段链断了四段:

- **13 件 kaleido 道具 `chamber_template_ids` 全为空数组 ⟹ 一件都搜不到**
- kaleido **没有专属 chamber**(25 个里 0 个)⟹ 复用多人房 ⟹ 玩家搜到的是**多人标定**的药,其中 **id22 结构修复包 `heal=300`**(挂 13 个房、高权重),而 **maxHp=100** ⟹ 一次捡到满血、还浪费 200
- event_deck 仍指多人旧 id(修它的 cadence SQL **被冻结了**)
- 6 种材料同样够不着 ⟹ **5 条配方全死链**

⟹ **`kaleido_bleed_per_action` 在链修好前不应打开** —— 打开就是两个极端(搜到 300 药包从此无风险 / 一件药都遇不上纯掉血等死),**中间那条算了三轮的曲线线上根本不存在**。

⟹ **修法要 🧭/Kanata 拍**(动到多人共享表):建 **kaleido 专属 chamber**(`spawn_weight=0` 不进多人 raidPath),13 件只挂它 —— **换池子比逐件调权重干净,同时解「够不着」与「多人 300 药包漏进来」两个问题**。
⚠ **前置未知**:kaleido 关卡 payload 里 `chamber_template_id` **全为 null**,即 kaleido 现在走多人 raidPath 分房 ⟹ **需 🔧 出机制**(⚙️ 只能提需求)。

#### 🔴🔴 另一条结构级发现(`4f56cfdd`):**step1 有两个制度,选哪个是定性不是调参**

每搜**净收支的符号**决定 step1 是哪一种游戏:

- **净赚(G≤12)= 带正漂移的赌徒破产** —— 破产概率**收敛到 ~15% 上限**,危险**全部前置在头 ~15 搜**,熬过去的玩家**结构性安全**且越搜越富。**没有时钟。**
- **净亏(G=16)= 真时钟** —— 死亡随时间趋向必然(L=100 时 85%),**必须离开**。

**这两个不是同一条曲线上的两个点,是两种游戏。**
⚙️ 仍推荐 G=10,但换了个更硬的理由:**时钟已经由死区提供**(Kanata 的第二口钟就是那个"必须回去"的强制力)⟹ 搜索经济不该再当第二个时钟,否则**两口钟打架**。⟹ **搜索经济负责「惊险但活得下来」,死区负责「必须回去」,各司一职。**

⚠ **但死区也未实现** ⟹ 这个论证目前是**一张期票**。**Kanata 拍死亡率取舍(§2)时必须连这条一起看** —— 它把问题从「0% 还是 3%」重新表述成了「step1 是哪一种游戏」。

### 2. ✅ 字段落点草案**已交** = `docs/plan/kaleido/14-field-layout-draft.md` —— **等 🧭 批四件**(§1 末)
已答:`offered` = `players[uid].uiOffered` **对象** `{[ui_key]: beat}`(诱饵要「未发现时长」)/ 提交定序 = 场景 → step → uiCommitted → **run 账本最后落**(四者介质不同 ⟹ **不可能单事务**,失败方向刻意选「宁可重复提交,不可丢」,前三者皆幂等)/ 「step 成功但 UI 失败」= **接受错位不补偿**(补偿会制造 step 回滚,而 step 定义是永不回滚)/ `step` = `profiles.kaleido_step` **独立 INT 列**(回滚语义相反的东西不共用字段)/ `kind` 六值,**只有 `unlock`/`hide`/`restore` 带顶层 `ui_key`** ⟹ 叙事类被今天客户端那行 `continue` 天然挡住。

### 3. P1 引擎内投影重构(按**存档点提交**语义)
账号列写入触发点 = **到达存档点写一次**,不是每动作。写源必须读独立命名的 `uiCommitted`,**严禁读渲染集**。

### 4. `kaleido_scene_state` 新表(DDL **已写待审**:`scripts/kaleido-scene-state.sql`·**未跑**)
⚠ **形状已终裁(2026-07-23)**:`restore_at timestamptz NOT NULL`,**「永不」= `'infinity'`**。
🔧 曾反对可空时间戳(NULL 在「未设置」与「永不」间有歧义,读错就把存档点锚重置了)—— **反对成立**,故不用 NULL;但两档枚举表达不了 Kanata 明说的「几天」。`'infinity'` 同时满足三条:无 NULL 歧义 / `DELETE WHERE restore_at <= now()` 对 `'infinity'` **结构上永不成立**(与枚举同级的保证)/ 「几天」= `now() + interval '3 days'`,加新周期零 schema 改动。
列注释须写明:**`'infinity'` = 永不复原,当前唯一持有者 = 安全屋的门(存档点锚)**。
**⚠ 🔧 后来撤回了自己的反对(「行只在写入时才存在 ⟹ NULL 只有一种含义」,成立),并改成了 NULL。但 🧭 实测后维持 `'infinity'`,理由换成新的一条:**

| | `restore_at <= now()`(重置作业) | `restore_at > now()`(读侧「仍有效」) |
|---|---|---|
| `'infinity'` | **false** ✓ 扫不到 | **true** ✓ 读得到 |
| `NULL` | 不匹配 ✓ 扫不到 | **不匹配** ✗ **永久行读不到** |

⟹ NULL 在删除侧安全,**在读侧会把永久行静默丢掉** —— 最自然的写法 `WHERE restore_at > now()` 会让门**永远读不出来**。🔧 最初担心的「把存档点锚弄丢」原样发生,只是从 DELETE 挪到 SELECT。**DDL 与 doc 13 须改回 `'infinity'`。**
已定取值:**门=永不** / 灯=当天 / 炸毁区块=数天。
**待 🧭 定**:重置作业形态。🔧 倾向**惰性到期 + 定期清理**(读侧本就要过滤 `restore_at <= now()` ⇒ 零新增基础设施、天然幂等;DELETE 只回收行，晚跑几小时无语义影响)，优于在 Vercel serverless 上跑一个**改游戏状态**的 cron。

### 4.5 `scripts/kaleido-scene-state.sql`(🔧 已出,**🧭 停工时刻意未审**)
新建表 + partial index + RLS 照 `content_pool` 范式。**不 ALTER 任何现有表 ⟹ 多人零变化。** 值得清醒时逐行读,**恢复后与 doc 14 四件同批审**。
- 已批的两件配套:**重置作业 = 惰性到期 + 定期清理**(不在 Vercel serverless 上跑改游戏状态的 cron,同教义 §8.4 顾虑);**stopgap(`content_pool` 新 entity_type)不做**(要改前端两处硬编码枚举且无 `(scene,prop)` 唯一键)。

### 5. P1-c 行为变更批(🎨 + 🔧 **必须同 PR**)
渲染判据可收缩 / 冷开场后全集重同步 / 移除·被夺的呈现语言 / `statusActionReady` 解耦成逐行门 / 未 discovered 要约的常驻载体。**半批上线 = 引擎与画面互相打脸。**

### 6. 其余队列
- 🔧:E2E 碰 profiles —— **做法已批 = service_role 直插 + 用后按 uid 清理**(`auth.admin.createUser` 会触发 `handle_new_user()` 的静默吞,且固定 tag 派生 email 重跑必撞 `profiles_username_key` 且无信号)/ `ITEM_COLS` 补三个 delta 列 + admin 内容引擎 kind 选项已坏(`itemPoolPreview.js:20,34`)
- ⚙️:`atk_delta`/`def_delta` 补值 —— **列已建(28 行全 0)· 补丁已出稿待审(`8763bb51`)**。条件:加力件/加防件旧 `atk`/`def` 归零;**id24 一律不动**
- 📖:N3 §6 三项 → 死区告知设计(**放在基地的剧情里**,不做元层弹窗)
- 🎨:~~SSR 直链顶栏闪~~ **已判定不治并记档**(`b0ff12cf`,含两条治法全文与代价 + **翻案条件 = 硬加载变成主路径,最可能触发点是 PWA**,🎨 自己盯)

### 7. 冻结(勿动)
`seq3-5 SQL` / `boss 曲线 260-34-8` / `cadence SQL` / `prep_readout` / `convergence_preview`。
**已救回**:`loadout_panel` 重锚到 step1 首次合成扩容件。

---

## 4. 已知风险 / 未验证

- **账号级持久化至今零 E2E 覆盖** —— E2E 用纯内存 uid,`profiles.id` FK 到 `auth.users`,那句 update 恒 0 行 no-op。而我们正要往账号层加存档点提交语义。
- **N7 兜底通道 `src/` 零实现** —— 法则一的公平性目前没有任何实现支撑。H1 通道设计稿已出(🔧,`docs/plan/kaleido/15`),未落地。
- **教义三条硬约束只有一条真在守** —— `check-doctrine-invariants.mjs` 在字段缺席时 PASS 并打印「⏳ 待字段」,**不假装在守**。字段一落地自动强制。
- ⚠ **📖 的对抗核验 workflow 因月度额度上限全部失败**,改纯自查。**精确边界(📖 自报):`b49c30b5` 之后的交付均为纯自查** —— N9 全文 / 选词法⑦ / N7 并发口径 / loadout_panel 提案 / 材料掉率复核。
**恢复时若额度通了,优先核 N9 §3 的四拍文案(要入库的字)与 §7.3 的概率推算(中位 23.9 搜 / 61.7% / 28.8%,单人算的数,没有第二双眼睛验过)。**(🧭 的 workflow 可能同样受限,恢复时先探一次。)
- `?kaleido=1` 直链硬加载仍有一次 SSR→hydration 顶栏闪 —— SSR 固有,已知未治。

---

## 5. 恢复时不用翻的铁律

- 只更新 `Readme_Claude`,**绝不动 `Readme_GPT` / `GPT.md`**;GPT 六件未碰
- `git add` **一律显式路径,严禁 `-A`**;`scripts/phase-25q-nemesis-pvp-death.sql` 保持 untracked
- 提交中文 + 尾签 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- 推 `git push origin claude/suspicious-solomon-598909:main`;推前 `git fetch origin && git rebase origin/main`
- **SQL 先审后经 postgres MCP 执行;DDL 必须先审**
- UI 改动**须 Kanata 线上实机验收**(各轨本地占位 env 验不了真实态)
- 📖 推送被 GPT 脏树挡 → 其提交由 🧭 cherry-pick 代传
- **多人局零行为变化**是红线;引用一律用 ID 不按名串
- 🧭 = 只拆解/派单/仲裁/验收,**不做具体开发**

### 两条本轮学到的方法论
- **转述另一轨的自述前,先核实产物真在仓库里。**(已犯两次:区间算错归属 / 把"跑过的检查"当成"提交了的脚本")
- **凡「修了 bug 顺手加断言」,都要跑一次负对照。** 本轮救了三次(ui_key 对拍 / H3 / 🔧 自曝的假断言)。
- **先问被测的东西在不在。**(⚙️ 提炼,本轮同形状犯了**三次**:加力件哑掉 / `CRAFT_MATERIAL_KINDS` 漏 `material` / 整条药品经济够不着)
  > 三次都是同一个形状:**验证了「值对不对」,没验证「链通不通」。**
  层级关系:先问**被测的东西在不在** → 再问**仪表对不对** → 最后才问**曲线对不对**。前两层任一为否,第三层算得再准也是空转。

---

## 6. 核心文档

| 文档 | 内容 |
|---|---|
| `docs/plan/kaleido/11-diegetic-ui-doctrine.md` | **长期铁律**。三法则 / 要约·发现四态两层 / 门类判据 / 存档点提交 / 结构重置+死区(§8) / 决策记录(§7) |
| `docs/plan/kaleido/12-step-progression.md` | step 推进结构。step=具名事件推进的账号标记 / 三条独立作用域 / 里程碑「关上门」/ 掉血起点与同率 |
| `docs/plan/kaleido/13-position-system-recon.md` | 🔧 位置系统与场景持久层勘察。**用「关上那扇门」走了一遍:七段里五段在关键路径上且全未落地 ⟹ step1 里程碑改口径后重了一个量级** |
| `docs/plan/kaleido/14-field-layout-draft.md` | 🔧 字段落点草案 —— **恢复后第一件事就是批它** |
| `docs/plan/kaleido/15-narrative-cue-channel.md` | 🔧 H1 叙事通道设计(原编号 12,与 step 结构撞号,🧭 已改 15) |
| `docs/narrative/kaleido-n1~n9` | 叙事资产(n8=多单位框架,n9=step 载体对齐) |
| `Claude/gameplay/step1-survival-curves.md` | ⚙️ 掉血/投药/L 敏感度 |
